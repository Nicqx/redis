apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ""   # Üres string, hogy ne várjon dinamikus provisionerre
  selector:
    matchLabels:
      type: manual
